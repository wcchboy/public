<?xml version="1.0" encoding="utf-8"?><!-- 圆角按钮 -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 圆角 -->
    <corners android:radius="3dp" />
    <!-- 对应渐变 -->
    <gradient
        android:angle="45"
        android:centerColor="@color/progress_bar_middle"
        android:endColor="@color/progress_bar_end"
        android:startColor="@color/progress_bar_start" />
</shape>
